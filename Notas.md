# Notas

## Amplify
1. Al desplegar el aplicativo en amplify asignar las variables de entorno son necesarias para el archivo `amplify-config`, estas variables se obtienen cuando se construye el backend serverless.
2. Si hay algun error de permisos revisar si el aplicativo tiene algún rol IAM.